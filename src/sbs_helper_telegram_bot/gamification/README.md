# Модуль геймификации

Модуль геймификации предоставляет систему достижений, очков и рейтингов для пользователей Telegram-бота.

## Содержание

- [Обзор](#обзор)
- [Возможности](#возможности)
- [Установка](#установка)
- [Структура модуля](#структура-модуля)
- [База данных](#база-данных)
- [Система событий](#система-событий)
- [Интеграция с модулями](#интеграция-с-модулями)
- [Пользовательский интерфейс](#пользовательский-интерфейс)
- [Админ-панель](#админ-панель)
- [Внешние скрипты](#внешние-скрипты)
- [Конфигурация](#конфигурация)

## Обзор

Система геймификации позволяет:
- Начислять очки за различные действия пользователей
- Выдавать достижения трёх уровней (бронза, серебро, золото)
- Присваивать ранги на основе накопленных очков
- Формировать рейтинги пользователей

## Возможности

### Для пользователей

- **Профиль** — просмотр своего ранга, очков и достижений
- **Достижения** — список всех полученных достижений с уровнями
- **Рейтинги** — таблицы лидеров по очкам и достижениям
- **Просмотр профилей** — возможность посмотреть профиль другого пользователя из рейтинга

### Для администраторов

- Просмотр профиля любого пользователя по ID или username
- Настройка очков за различные действия
- Просмотр списка всех достижений
- Статистика системы

## Установка

### 1. Создание таблиц базы данных

```bash
mysql -u <user> -p <database> < scripts/gamification_setup.sql
```

### 2. Регистрация обработчиков

Обработчики уже зарегистрированы в `telegram_bot.py`:

```python
from src.sbs_helper_telegram_bot.gamification import (
    get_gamification_user_handler,
    get_gamification_admin_handler,
)

# В функции register_handlers:
application.add_handler(get_gamification_admin_handler())
application.add_handler(get_gamification_user_handler())
```

## Структура модуля

```
gamification/
├── __init__.py              # Экспорт публичных функций
├── settings.py              # Константы, состояния, кнопки
├── messages.py              # Тексты сообщений (RU)
├── keyboards.py             # Клавиатуры (reply и inline)
├── events.py                # Шина событий
├── gamification_logic.py    # Бизнес-логика
├── gamification_bot_part.py # Обработчики пользователей
├── admin_panel_bot_part.py  # Обработчики админов
└── README.md                # Документация
```

## База данных

### Таблицы

| Таблица | Описание |
|---------|----------|
| `gamification_achievements` | Определения достижений (пороги, описания) |
| `gamification_user_progress` | Прогресс пользователей к достижениям |
| `gamification_user_achievements` | Полученные достижения |
| `gamification_scores` | Лог начисления очков |
| `gamification_user_totals` | Кэш итогов (очки, достижения) |
| `gamification_events` | Лог событий |
| `gamification_settings` | Системные настройки |
| `gamification_score_config` | Настройка очков за действия |

### Схема достижений

Каждое достижение имеет три уровня:

| Уровень | Константа | Описание |
|---------|-----------|----------|
| Бронза | `ACHIEVEMENT_LEVEL_BRONZE = 1` | Начальный уровень |
| Серебро | `ACHIEVEMENT_LEVEL_SILVER = 2` | Средний уровень |
| Золото | `ACHIEVEMENT_LEVEL_GOLD = 3` | Максимальный уровень |

Пороги задаются индивидуально для каждого достижения в полях `threshold_bronze`, `threshold_silver`, `threshold_gold`.

### Ранги

По умолчанию настроено 5 рангов:

| Ранг | Очки от |
|------|---------|
| 🌱 Новичок | 0 |
| ⭐ Специалист | 100 |
| 🏅 Эксперт | 500 |
| 🎖️ Мастер | 1500 |
| 👑 Легенда | 5000 |

Ранги настраиваются в таблице `gamification_settings` с ключами `rank_1` ... `rank_5`.

## Система событий

Модуль использует шину событий для слабой связанности. Другие модули вызывают `emit_event()`, а система геймификации обрабатывает события.

### Использование

```python
from src.sbs_helper_telegram_bot.gamification import emit_event

# В обработчике модуля:
await emit_event("ktr.lookup", user_id, {"code": code})
```

### Регистрация событий

В файле `events.py` регистрируются обработчики:

```python
# Встроенные события
register_event(
    event_type="ktr.lookup",
    achievement_code="ktr_lookup",      # Код достижения для инкремента
    score_action="ktr_lookup"           # Действие для начисления очков
)

# Кастомные обработчики
register_custom_handler("ktr.daily_user", handle_ktr_daily_user)
```

### Зарегистрированные события

| Событие | Достижение | Очки |
|---------|------------|------|
| `ktr.lookup` | `ktr_lookup` | `ktr_lookup` |
| `ktr.lookup_found` | `ktr_lookup_found` | `ktr_lookup_found` |

## Интеграция с модулями

### Шаг 1: Добавить emit_event() в модуль

```python
from src.sbs_helper_telegram_bot.gamification import emit_event

async def some_handler(update, context):
    # ... логика модуля ...
    
    # Отправить событие
    await emit_event("module.action", user_id, {"key": "value"})
```

### Шаг 2: Зарегистрировать событие

В `gamification/events.py`:

```python
register_event(
    event_type="module.action",
    achievement_code="module_action",  # должен существовать в БД
    score_action="module_action"       # должен существовать в БД
)
```

### Шаг 3: Добавить данные в БД

```sql
-- Достижение
INSERT INTO gamification_achievements 
    (code, module, name, description, threshold_bronze, threshold_silver, threshold_gold)
VALUES 
    ('module_action', 'module', 'Название', 'Описание', 10, 50, 100);

-- Очки за действие
INSERT INTO gamification_score_config (action_code, module, points, description)
VALUES ('module_action', 'module', 5, 'Описание действия');
```

### Шаг 4: Добавить кнопку достижений (опционально)

В `settings.py` модуля добавить кнопку:

```python
SUBMENU_BUTTONS = [
    # ... существующие кнопки ...
    "🎖️ Достижения"
]
```

В `bot_part.py` модуля добавить обработчик:

```python
from src.sbs_helper_telegram_bot.gamification import (
    get_user_achievements_by_module,
    format_achievements_message
)

async def show_achievements(update, context):
    user_id = update.effective_user.id
    achievements = await get_user_achievements_by_module(user_id, "module_name")
    message = format_achievements_message(achievements)
    await update.message.reply_text(message, parse_mode="MarkdownV2")
```

## Пользовательский интерфейс

### Главное меню

Кнопка **🏆 Профиль** в главном меню открывает подменю:

- **👤 Мой профиль** — ранг, очки, статистика
- **🎖️ Мои достижения** — список полученных достижений
- **📊 Рейтинги** — таблицы лидеров
- **🔍 Найти пользователя** — поиск по username

### Рейтинги

Два типа рейтингов:
- **По очкам** — сортировка по сумме очков
- **По достижениям** — сортировка по количеству достижений

Три периода:
- **За месяц** — текущий календарный месяц
- **За год** — текущий календарный год
- **За всё время** — без ограничений

### Профиль пользователя

```
👤 Ваш профиль

🎖️ Ранг: ⭐ Специалист
💎 Очков: 250
🏆 Достижений: 5

📈 Статистика:
• За месяц: +45 очков
• За год: +200 очков
```

## Админ-панель

Вход через админ-меню бота → **🎮 Геймификация**

### Функции

| Кнопка | Описание |
|--------|----------|
| 🔍 Найти профиль | Поиск пользователя по ID или username |
| ⚙️ Настройка очков | Изменение очков за действия |
| 🎖️ Все достижения | Список всех достижений в системе |
| 📊 Статистика | Общая статистика системы |

### Права администраторов

- ✅ Просмотр профилей пользователей
- ✅ Настройка очков за действия
- ✅ Просмотр статистики
- ❌ Ручное начисление очков (только через скрипты)
- ❌ Выдача/отзыв достижений

## Внешние скрипты

### add_daily_scores.py

Скрипт для массового начисления очков из командной строки.

```bash
# Одному пользователю
python scripts/add_daily_scores.py --userid 123456789 --points 10 --reason "Бонус"

# Списку из файла (по одному user_id на строку)
python scripts/add_daily_scores.py --file users.txt --points 5 --reason "Ежедневный бонус"

# Всем активным пользователям
python scripts/add_daily_scores.py --all-active --points 1 --reason "Активность"

# Тестовый запуск (без изменений в БД)
python scripts/add_daily_scores.py --all-active --points 10 --dry-run
```

#### Параметры

| Параметр | Описание |
|----------|----------|
| `--userid` | ID одного пользователя |
| `--file` | Файл со списком user_id |
| `--all-active` | Все пользователи с очками > 0 |
| `--points` | Количество очков (обязательно) |
| `--reason` | Причина начисления |
| `--source` | Источник (по умолчанию "script") |
| `--dry-run` | Тестовый режим |

### Программное начисление очков

```python
from src.sbs_helper_telegram_bot.gamification import add_score_points

# Начислить очки
await add_score_points(
    userid=123456789,
    points=10,
    source="custom_script",
    reason="Описание причины"
)
```

## Конфигурация

### Настройка очков за действия

Таблица `gamification_score_config`:

```sql
-- Просмотр текущих настроек
SELECT * FROM gamification_score_config;

-- Изменение очков
UPDATE gamification_score_config SET points = 10 WHERE action_code = 'ktr_lookup';
```

### Настройка рангов

Таблица `gamification_settings`:

```sql
-- Изменить порог для ранга
UPDATE gamification_settings 
SET setting_value = '{"name": "⭐ Специалист", "min_score": 200}' 
WHERE setting_key = 'rank_2';
```

### Настройка порогов достижений

Таблица `gamification_achievements`:

```sql
-- Изменить пороги для достижения
UPDATE gamification_achievements 
SET threshold_bronze = 5, threshold_silver = 25, threshold_gold = 100 
WHERE code = 'ktr_lookup';
```

## Расширение системы

### Добавление нового достижения

1. Добавить запись в `gamification_achievements`
2. Зарегистрировать событие в `events.py`
3. Вызывать `emit_event()` в нужном месте модуля

### Добавление нового ранга

Добавить запись в `gamification_settings` с ключом `rank_N`:

```sql
INSERT INTO gamification_settings (setting_key, setting_value)
VALUES ('rank_6', '{"name": "🌟 Грандмастер", "min_score": 10000}');
```

### Добавление изображений достижений

В таблице `gamification_achievements` есть поля для изображений:

- `image_bronze` — путь к изображению бронзового уровня
- `image_silver` — путь к изображению серебряного уровня  
- `image_gold` — путь к изображению золотого уровня

```sql
UPDATE gamification_achievements 
SET image_gold = 'assets/achievements/ktr_master.png' 
WHERE code = 'ktr_lookup';
```

## Примеры запросов

### Топ-10 пользователей по очкам

```sql
SELECT u.userid, u.username, t.total_score, t.total_achievements
FROM gamification_user_totals t
JOIN telegram_users u ON t.userid = u.userid
ORDER BY t.total_score DESC
LIMIT 10;
```

### Пользователи с золотым достижением

```sql
SELECT u.userid, u.username, a.name
FROM gamification_user_achievements ua
JOIN telegram_users u ON ua.userid = u.userid
JOIN gamification_achievements a ON ua.achievement_id = a.id
WHERE ua.level = 3;
```

### Статистика за месяц

```sql
SELECT 
    COUNT(DISTINCT userid) as active_users,
    SUM(points) as total_points,
    COUNT(*) as transactions
FROM gamification_scores
WHERE created_at >= DATE_FORMAT(NOW(), '%Y-%m-01');
```
