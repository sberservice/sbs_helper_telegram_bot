# Ticket Validator Module

Модуль проверки заявок для телеграм-бота инженеров СберСервис.

## Описание

Модуль позволяет инженерам проверять правильность заполнения заявок перед отправкой. Бот автоматически определяет тип заявки по ключевым словам и применяет соответствующие правила валидации.

## Возможности

- ✅ Автоматическое определение типа заявки по ключевым словам
- ✅ Валидация по типо-специфичным правилам
- ✅ Поддержка негативных ключевых слов (снижают счёт определения)
- ✅ Настраиваемые веса ключевых слов (case-insensitive)
- ✅ Различные типы правил: regex, required_field, format, length, custom
- ✅ История проверок для каждого пользователя
- ✅ Админ-панель для управления правилами и типами
- ✅ Тестовые шаблоны для проверки правил валидации

## Команды бота

| Команда | Описание |
|---------|----------|
| `/validate` | Начать проверку заявки |
| `/help_validate` | Справка по использованию |
| `/cancel` | Отменить текущую проверку |
| `/admin` | Админ-панель (только для администраторов) |

## Установка

### 1. Создать схему базы данных

```bash
mysql -u root -p < schema.sql
```

### 2. Загрузить начальные данные

```bash
mysql -u root -p sprint_db < scripts/initial_ticket_types.sql
mysql -u root -p sprint_db < scripts/initial_validation_rules.sql
mysql -u root -p sprint_db < scripts/map_rules_to_ticket_types.sql
mysql -u root -p sprint_db < scripts/sample_templates.sql
```

## Использование

### Для пользователей

1. Отправьте `/validate`
2. Скопируйте текст заявки и отправьте боту
3. Получите результат проверки
4. При необходимости исправьте ошибки и повторите

### Пример валидной заявки

```
Заявка на установку оборудования

Наименование организации: ООО "Пример"
ИНН: 1234567890
Система налогообложения: УСН

Контактное лицо: Иванов Иван Иванович
Контактный телефон: +7 (999) 123-45-67

Адрес установки: г. Москва, ул. Примерная, д. 1, оф. 100
Дата обслуживания: 20.01.2026

Тип оборудования: Касса АТОЛ 91Ф
Код активации: ABC123DEF456
```

## Администрирование

### Доступ к админ-панели

```sql
-- Сделать пользователя администратором
UPDATE users SET is_admin = 1 WHERE userid = <telegram_user_id>;
```

Затем в боте: `/admin`

### Функции админ-панели

- **📋 Правила** — CRUD операции с правилами валидации
- **📁 Типы заявок** — управление типами и ключевыми словами
- **🧪 Тест шаблоны** — шаблоны для тестирования правил
- **🔬 Тест regex** — интерактивное тестирование regex-паттернов

## Типы правил валидации

| Тип | Описание | Пример pattern |
|-----|----------|----------------|
| `regex` | Регулярное выражение | `(?i)ИНН\s*:\s*\d{10,12}` |
| `required_field` | Обязательное поле | `Система налогообложения` |
| `format` | Специальный формат | `phone`, `email`, `date`, `inn` |
| `length` | Ограничение длины | `min:50,max:5000` |
| `custom` | Пользовательская логика | — |

## Структура модуля

```
ticket_validator/
├── __init__.py
├── validators.py           # Логика валидации, TicketType, ValidationRule
├── validation_rules.py     # Работа с БД
├── ticket_validator_bot_part.py  # Обработчики пользовательских команд
├── admin_panel_bot_part.py      # Админ-панель
├── keyboards.py            # Клавиатуры
├── messages.py             # Сообщения
├── settings.py             # Настройки модуля
├── README.md               # Этот файл
├── TICKET_TYPES.md         # Документация по типам заявок
├── NEGATIVE_KEYWORDS.md    # Документация по негативным ключевым словам
└── TEST_TEMPLATES.md       # Документация по тестовым шаблонам
```

## Документация

- [Типы заявок](TICKET_TYPES.md) — система автоматического определения типов
- [Негативные ключевые слова](NEGATIVE_KEYWORDS.md) — ключевые слова с отрицательным весом
- [Тестовые шаблоны](TEST_TEMPLATES.md) — система тестирования правил

## API функций

### validators.py

| Функция | Описание |
|---------|----------|
| `validate_ticket(text, rules, ticket_type)` | Основная валидация |
| `detect_ticket_type(text, types, debug, weights)` | Определение типа заявки |
| `validate_regex(text, pattern)` | Проверка regex |
| `validate_format(text, format_type)` | Проверка формата |
| `validate_length(text, spec)` | Проверка длины |

### validation_rules.py

| Функция | Описание |
|---------|----------|
| `load_all_ticket_types()` | Загрузить активные типы |
| `load_rules_from_db(ticket_type_id)` | Загрузить правила |
| `store_validation_result(...)` | Сохранить результат |
| `get_validation_history(userid, limit)` | История проверок |
| `create_validation_rule(...)` | Создать правило |
| `update_validation_rule(...)` | Обновить правило |
| `toggle_validation_rule(id)` | Вкл/выкл правило |

## Особенности

### Негативные ключевые слова

Ключевые слова с префиксом `-` снижают счёт при определении типа:

```json
["установка", "монтаж", "-ремонт", "-замена"]
```

### Веса ключевых слов

Можно задать веса для более точного определения (case-insensitive):

```json
{
  "установка": 2.0,
  "монтаж": 1.5,
  "-ремонт": 1.0
}
```

### Тестовые шаблоны

Админы могут создавать шаблоны с ожидаемыми результатами валидации для автоматического тестирования правил.

## Безопасность

- ✅ Параметризованные SQL-запросы
- ✅ Обработка ошибок regex
- ✅ История привязана к userid
- ✅ Админ-функции только для is_admin=1

---

**Версия:** 2.0.0  
**Обновлено:** Январь 2026
