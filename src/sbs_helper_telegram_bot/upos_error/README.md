# 🔢 UPOS Ошибки

Модуль для поиска кодов ошибок системы UPOS с подробными описаниями и рекомендациями по устранению.

## 📋 Содержание

- [О модуле](#о-модуле)
- [Возможности](#возможности)
- [Установка и настройка](#установка-и-настройка)
- [Руководство пользователя](#руководство-пользователя)
- [Руководство администратора](#руководство-администратора)
- [Структура базы данных](#структура-базы-данных)
- [Структура файлов](#структура-файлов)
- [Примеры использования](#примеры-использования)
- [FAQ](#faq)

## О модуле

**UPOS Ошибки** — это модуль Telegram-бота, который предоставляет:

- **Быстрый поиск** кодов ошибок UPOS с подробными описаниями
- **Рекомендации по устранению** для каждой ошибки
- **Категоризацию** ошибок (Принтер, Сеть, Касса, Авторизация и т.д.)
- **Статистику запросов** для анализа наиболее проблемных ошибок
- **Автоматическое отслеживание** неизвестных кодов
- **Административную панель** для управления базой знаний

### Назначение

Модуль помогает сотрудникам быстро находить решения проблем с системой UPOS без необходимости обращаться к документации или в техподдержку. Администраторы могут оперативно пополнять и актуализировать базу знаний.

## Возможности

### Для пользователей

- 🔍 **Поиск кодов ошибок** — введите код и получите описание с рекомендациями
- 📊 **Просмотр популярных ошибок** — топ-10 наиболее запрашиваемых кодов
- 📁 **Категории ошибок** — ошибки организованы по типам (Принтер, Сеть, Касса и др.)
- 📅 **Актуальность информации** — отображается дата последнего обновления рекомендаций

### Для администраторов

- ➕ **Добавление новых кодов ошибок** с описаниями и рекомендациями
- ✏️ **Редактирование существующих записей** — обновление описаний и рекомендаций
- 📁 **Управление категориями** — создание, редактирование, удаление категорий
- ❓ **Обработка неизвестных кодов** — автоматический список кодов, которые искали пользователи
- 📈 **Статистика** — количество запросов, топ кодов, процент найденных ошибок
- 🚫 **Деактивация/активация** кодов без удаления из базы
- 🗑️ **Удаление** ошибок и категорий

## Установка и настройка

### 1. Создание таблиц базы данных

Выполните SQL-скрипт для создания необходимых таблиц:

```bash
mysql -u username -p database_name < scripts/upos_error_setup.sql
```

Или выполните скрипт вручную из файла [scripts/upos_error_setup.sql](../../../../scripts/upos_error_setup.sql).

### 2. Настройка модуля

Параметры модуля настраиваются в файле [settings.py](settings.py):

```python
# Метаданные модуля
MODULE_NAME = "UPOS Ошибки"
MODULE_VERSION = "1.0.0"

# Пагинация
ERRORS_PER_PAGE = 10           # Ошибок на страницу в админке
CATEGORIES_PER_PAGE = 10       # Категорий на страницу
UNKNOWN_CODES_PER_PAGE = 15    # Неизвестных кодов на страницу
TOP_POPULAR_COUNT = 10         # Количество популярных ошибок
```

### 3. Интеграция с ботом

Модуль автоматически интегрируется с главным ботом. Убедитесь, что обработчики зарегистрированы в главном файле бота.

### 4. Начальные данные

Рекомендуется добавить базовые категории и коды ошибок через админ-панель или SQL-запросами:

```sql
-- Добавление категорий
INSERT INTO upos_error_categories (name, description, display_order, created_timestamp) VALUES
('Принтер', 'Ошибки печати и работы принтера', 1, UNIX_TIMESTAMP()),
('Сеть', 'Проблемы сетевого подключения', 2, UNIX_TIMESTAMP()),
('Касса', 'Ошибки кассовых операций', 3, UNIX_TIMESTAMP()),
('Авторизация', 'Проблемы входа и авторизации', 4, UNIX_TIMESTAMP()),
('Общие', 'Общие ошибки системы', 99, UNIX_TIMESTAMP());

-- Добавление примера кода ошибки
INSERT INTO upos_error_codes (error_code, category_id, description, suggested_actions, created_timestamp) VALUES
('101', 1, 'Нет бумаги в принтере', '1. Откройте крышку принтера\n2. Установите новый рулон бумаги\n3. Закройте крышку и повторите операцию', UNIX_TIMESTAMP());
```

## Руководство пользователя

### Поиск кода ошибки

1. В главном меню бота нажмите **🔢 UPOS Ошибки**
2. Выберите **🔍 Найти код ошибки**
3. Введите код ошибки (например, `101` или `E-001`)
4. Получите результат:
   - Код ошибки
   - Категория (если назначена)
   - Подробное описание
   - Рекомендации по устранению
   - Дата обновления (если была)

**Если код не найден:**
- Бот сообщит, что код отсутствует в базе
- Ваш запрос автоматически записывается для администраторов
- Администраторы смогут добавить описание этого кода

### Просмотр популярных ошибок

1. В подменю **UPOS Ошибки** нажмите **📊 Популярные ошибки**
2. Увидите топ-10 наиболее запрашиваемых кодов с кратким описанием

## Руководство администратора

### Доступ к админ-панели

Администраторы видят дополнительную кнопку **🔐 Админ UPOS** в подменю модуля.

### Добавление нового кода ошибки

1. **🔐 Админ UPOS** → **➕ Добавить ошибку**
2. Введите **код ошибки** (например, `E-500`)
3. Введите **описание ошибки**
4. Введите **рекомендации по устранению** (можно использовать нумерованный список)
5. Выберите **категорию** из списка или пропустите (нажмите "Пропустить")
6. Система создаст новую запись с текущей датой

**Примечание:** Если код уже существует, система предложит отредактировать существующую запись.

### Редактирование кода ошибки

1. **🔐 Админ UPOS** → **📋 Список ошибок**
2. Выберите нужный код из списка (используйте пагинацию при необходимости)
3. Нажмите на код ошибки для просмотра деталей
4. Доступные действия:
   - **📝 Описание** — изменить описание ошибки
   - **💡 Рекомендации** — обновить рекомендации (с опцией обновления даты)
   - **📁 Категория** — изменить или назначить категорию
   - **🚫 Деактивировать** — скрыть код от пользователей (можно активировать обратно)
   - **🗑️ Удалить** — полностью удалить код из базы (требуется подтверждение)

**Обновление даты изменения:**
- При редактировании **рекомендаций** система спросит, обновлять ли дату
- **Да** — пользователи увидят актуальную дату обновления
- **Нет** — дата останется прежней (подходит для исправления опечаток)

### Управление категориями

1. **🔐 Админ UPOS** → **📁 Категории**
2. Просмотр списка категорий с количеством ошибок в каждой
3. Действия:
   - **Просмотр деталей** — нажмите на название категории
   - **Редактирование** — изменение названия, описания или порядка отображения
   - **Удаление** — категория удаляется, ошибки остаются без категории (`category_id = NULL`)

**Добавление категории:**
1. В меню категорий нажмите **➕ Добавить категорию**
2. Введите **название** категории
3. Введите **описание** (или `-` для пропуска)
4. Введите **порядок отображения** (число, меньше значение = выше в списке)

**Примеры порядка отображения:**
- `1` — Принтер (самая важная)
- `2` — Сеть
- `3` — Касса
- `99` — Общие (в конце списка)

### Обработка неизвестных кодов

1. **🔐 Админ UPOS** → **❓ Неизвестные коды**
2. Просмотр списка кодов, которые искали пользователи, но не нашли
3. Информация по каждому коду:
   - Код ошибки
   - Количество запросов
   - Дата последнего запроса
4. Нажмите **➕ [код]** для быстрого добавления описания
   - Код автоматически предзаполнен
   - После добавления запись удаляется из списка неизвестных

**Преимущество:** Вы сразу видите, какие ошибки актуальны и требуют добавления в базу.

### Просмотр статистики

1. **🔐 Админ UPOS** → **📈 Статистика**
2. Отображаемая информация:

**Общее количество:**
- Кодов ошибок в базе
- Активных категорий
- Неизвестных кодов

**За последние 7 дней:**
- Всего запросов пользователей
- Найдено в базе
- Не найдено в базе
- Процент покрытия

**Топ-5 запрашиваемых кодов:**
- Список наиболее популярных ошибок с количеством запросов

**Использование статистики:**
- Оценка полноты базы знаний (% найденных кодов)
- Определение проблемных зон (топ кодов)
- Мониторинг активности пользователей

## Структура базы данных

Модуль использует 4 таблицы:

### upos_error_codes

**Назначение:** Хранение кодов ошибок с описаниями и рекомендациями

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | bigint(20) | Первичный ключ |
| `error_code` | varchar(50) | Код ошибки (уникальный) |
| `category_id` | bigint(20) | Ссылка на категорию (может быть NULL) |
| `description` | text | Описание ошибки |
| `suggested_actions` | text | Рекомендации по устранению |
| `active` | tinyint(1) | Активен ли код (1 = да, 0 = нет) |
| `created_timestamp` | bigint(20) | Unix timestamp создания |
| `updated_timestamp` | bigint(20) | Unix timestamp обновления |

**Индексы:** `error_code` (UNIQUE), `category_id`, `active`

### upos_error_categories

**Назначение:** Категории для группировки ошибок

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | bigint(20) | Первичный ключ |
| `name` | varchar(100) | Название категории (уникальное) |
| `description` | text | Описание категории |
| `display_order` | int(11) | Порядок отображения (меньше = выше) |
| `active` | tinyint(1) | Активна ли категория |
| `created_timestamp` | bigint(20) | Unix timestamp создания |
| `updated_timestamp` | bigint(20) | Unix timestamp обновления |

**Индексы:** `name` (UNIQUE), `active`, `display_order`

### upos_error_unknown_codes

**Назначение:** Отслеживание кодов, которых нет в базе, но которые искали пользователи

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | bigint(20) | Первичный ключ |
| `error_code` | varchar(50) | Неизвестный код (уникальный) |
| `times_requested` | int(11) | Количество запросов |
| `first_requested_timestamp` | bigint(20) | Unix timestamp первого запроса |
| `last_requested_timestamp` | bigint(20) | Unix timestamp последнего запроса |

**Индексы:** `error_code` (UNIQUE)

**Логика работы:**
- При каждом запросе несуществующего кода `times_requested` увеличивается на 1
- Администраторы видят список отсортированный по популярности

### upos_error_request_log

**Назначение:** Логирование всех запросов для статистики

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | bigint(20) | Первичный ключ |
| `user_id` | bigint(20) | Telegram ID пользователя |
| `error_code` | varchar(50) | Запрошенный код |
| `found` | tinyint(1) | Найден ли код (1 = да, 0 = нет) |
| `request_timestamp` | bigint(20) | Unix timestamp запроса |

**Индексы:** `user_id`, `error_code`, `request_timestamp`

**Использование:**
- Построение статистики за период
- Определение топ запрашиваемых кодов
- Анализ активности пользователей

### Связи таблиц

```
upos_error_categories (1) ──< (N) upos_error_codes
    └─ ON DELETE SET NULL (ошибки остаются без категории)

upos_error_request_log
    └─ Логирует все запросы, связь по error_code

upos_error_unknown_codes
    └─ Накапливает статистику по отсутствующим кодам
```

## Структура файлов

```
upos_error/
├── __init__.py                    # Инициализация модуля
├── README.md                      # Данная документация
├── settings.py                    # Настройки модуля
├── keyboards.py                   # Клавиатуры Telegram (ReplyKeyboard и InlineKeyboard)
├── messages.py                    # Сообщения бота в формате MarkdownV2
└── upos_error_bot_part.py         # Основная логика, обработчики, работа с БД
```

### upos_error_bot_part.py (1456 строк)

**Основной файл модуля**, содержащий:

**Функции работы с базой данных:**
- `get_error_by_code()` — поиск кода ошибки
- `get_error_by_id()` — получение ошибки по ID
- `get_errors_paginated()` — постраничный список
- `create_error_code()` — создание нового кода
- `update_error_code()` — обновление данных
- `delete_error_code()` — удаление
- `get_categories_paginated()` — список категорий
- `create_category()` — создание категории
- `update_category()` — обновление категории
- `delete_category()` — удаление категории
- `log_error_request()` — логирование запроса
- `record_unknown_code()` — запись неизвестного кода
- `get_unknown_codes()` — список неизвестных кодов
- `get_top_popular_errors()` — топ популярных ошибок
- `get_module_statistics()` — общая статистика

**Обработчики пользователя:**
- `start_upos_error()` — точка входа в модуль
- `search_error_code()` — начало поиска
- `process_error_code_input()` — обработка введённого кода
- `show_popular_errors()` — показ популярных ошибок

**Обработчики администратора:**
- `admin_menu()` — главное админ-меню
- `admin_list_errors()` — список всех ошибок
- `admin_add_error_start()` — начало добавления
- `admin_view_error_details()` — детальный просмотр
- `admin_edit_error()` — редактирование
- `admin_delete_error()` — удаление
- `admin_list_categories()` — список категорий
- `admin_add_category()` — добавление категории
- `admin_unknown_codes()` — неизвестные коды
- `admin_statistics()` — статистика модуля

**ConversationHandlers:**
- Пользовательский (состояния: `WAITING_FOR_ERROR_CODE`)
- Админский (13 состояний от `ADMIN_MENU` до `ADMIN_CONFIRM_UPDATE_DATE`)

### keyboards.py (365 строк)

**Все клавиатуры модуля:**

**ReplyKeyboardMarkup:**
- `get_upos_submenu()` — подменю для пользователей
- `get_upos_admin_submenu()` — подменю с админ-кнопкой
- `get_admin_menu_keyboard()` — главное меню админки
- `get_admin_category_menu_keyboard()` — меню категорий
- `get_admin_error_menu_keyboard()` — меню ошибок

**InlineKeyboardMarkup:**
- `create_errors_list_keyboard()` — список ошибок с пагинацией
- `create_categories_list_keyboard()` — список категорий
- `create_error_actions_keyboard()` — действия над ошибкой
- `create_category_actions_keyboard()` — действия над категорией
- `create_unknown_codes_keyboard()` — неизвестные коды
- `create_confirmation_keyboard()` — подтверждение Да/Нет

### messages.py (235 строк)

**Все текстовые сообщения в формате MarkdownV2:**

- Сообщения пользователю (приветствие, запрос кода, код не найден и т.д.)
- Сообщения администратору (меню, формы, подтверждения)
- Функции форматирования (`format_error_full()`, `format_error_list_item()`, и т.д.)
- `escape_markdown()` — экранирование специальных символов Telegram

### settings.py (58 строк)

**Константы и настройки модуля:**

- Метаданные (название, версия, автор)
- Тексты кнопок меню
- Параметры пагинации
- Количество элементов на страницу

## Примеры использования

### Пример 1: Пользователь ищет ошибку 101

**Диалог с ботом:**

```
👤 Пользователь: [нажимает "🔢 UPOS Ошибки"]

🤖 Бот: 
🔢 UPOS Ошибки

Выберите действие:
🔍 Найти код ошибки — поиск по коду
📊 Популярные ошибки — топ запрашиваемых кодов

👤 Пользователь: [нажимает "🔍 Найти код ошибки"]

🤖 Бот: Введите код ошибки UPOS

👤 Пользователь: 101

🤖 Бот:
🔢 Код ошибки: 101
📁 Категория: Принтер

📋 Описание:
Нет бумаги в принтере

💡 Рекомендации по устранению:
1. Откройте крышку принтера
2. Установите новый рулон бумаги
3. Закройте крышку и повторите операцию

📅 Обновлено: 15.01.2026
```

### Пример 2: Администратор добавляет новую ошибку

**Диалог с ботом:**

```
👤 Админ: [нажимает "🔐 Админ UPOS" → "➕ Добавить ошибку"]

🤖 Бот: Введите код ошибки

👤 Админ: E-555

🤖 Бот: Введите описание для кода E-555

👤 Админ: Ошибка связи с фискальным накопителем

🤖 Бот: Введите рекомендации по устранению для кода E-555

👤 Админ: 
1. Перезагрузите UPOS (Меню → Перезагрузка)
2. Проверьте подключение фискального накопителя
3. Убедитесь, что срок действия ФН не истёк
4. Обратитесь в техподдержку

🤖 Бот: Выберите категорию для кода E-555 или пропустите
[показывает inline-кнопки категорий]

👤 Админ: [выбирает "Касса"]

🤖 Бот: 
✅ Код ошибки успешно добавлен!

Код: E-555
Категория: Касса
Дата создания: 26.01.2026
```

### Пример 3: Работа с неизвестными кодами

**Сценарий:**
1. Пользователь ищет код `ZZZ-999` — не найден
2. Код автоматически попадает в таблицу `upos_error_unknown_codes`
3. Ещё 5 пользователей ищут этот же код
4. Администратор видит в **❓ Неизвестные коды**:

```
Код ZZZ-999
Запросов: 6
Последний запрос: 26.01.2026 14:30
[➕ Добавить] [🗑️ Удалить]
```

5. Админ нажимает **➕ Добавить** → код предзаполнен → добавляет описание
6. Запись автоматически удаляется из списка неизвестных

## FAQ

### Как массово импортировать коды ошибок?

Используйте SQL-запросы для вставки данных:

```sql
INSERT INTO upos_error_codes (error_code, category_id, description, suggested_actions, created_timestamp) VALUES
('201', 2, 'Нет связи с сервером', 'Проверьте интернет-соединение и повторите попытку', UNIX_TIMESTAMP()),
('202', 2, 'Таймаут подключения', 'Обратитесь к системному администратору', UNIX_TIMESTAMP()),
('301', 3, 'Смена не открыта', 'Откройте смену в UPOS перед началом работы', UNIX_TIMESTAMP());
```

Или подготовьте CSV-файл и импортируйте через MySQL:

```sql
LOAD DATA INFILE '/path/to/errors.csv'
INTO TABLE upos_error_codes
FIELDS TERMINATED BY ','
ENCLOSED BY '"'
LINES TERMINATED BY '\n'
IGNORE 1 ROWS
(error_code, category_id, description, suggested_actions, @created)
SET created_timestamp = UNIX_TIMESTAMP();
```

### Можно ли экспортировать базу знаний?

Да, используйте SQL-запрос:

```sql
SELECT 
    ec.error_code,
    cat.name AS category,
    ec.description,
    ec.suggested_actions,
    FROM_UNIXTIME(ec.created_timestamp) AS created,
    FROM_UNIXTIME(ec.updated_timestamp) AS updated
FROM upos_error_codes ec
LEFT JOIN upos_error_categories cat ON ec.category_id = cat.id
WHERE ec.active = 1
ORDER BY ec.error_code;
```

Или экспорт в CSV:

```sql
SELECT * FROM upos_error_codes
INTO OUTFILE '/tmp/upos_errors_backup.csv'
FIELDS TERMINATED BY ','
ENCLOSED BY '"'
LINES TERMINATED BY '\n';
```

### Как сбросить статистику запросов?

**Сброс логов запросов:**

```sql
TRUNCATE TABLE upos_error_request_log;
```

**Сброс неизвестных кодов:**

```sql
TRUNCATE TABLE upos_error_unknown_codes;
```

**Внимание:** Статистика будет потеряна безвозвратно. Рекомендуется сделать резервную копию:

```bash
mysqldump -u username -p database_name upos_error_request_log > backup_logs.sql
```

### Что происходит при удалении категории?

При удалении категории коды ошибок **НЕ удаляются**. Благодаря `ON DELETE SET NULL` их `category_id` становится `NULL` (без категории). Вы можете потом назначить им другую категорию.

### Можно ли деактивировать код вместо удаления?

Да! В детальном просмотре ошибки нажмите **🚫 Деактивировать**. Код останется в базе, но пользователи его не увидят. Администраторы могут активировать его обратно.

### Как изменить порядок отображения категорий?

В админ-панели:
1. **📁 Категории** → выберите категорию
2. **Редактировать** → измените **порядок отображения**

Категории с меньшим значением `display_order` показываются выше.

### Поддерживаются ли многострочные рекомендации?

Да! При вводе рекомендаций можно использовать многострочный текст с нумерованными или маркированными списками. Telegram отображает их корректно.

**Пример:**

```
1. Первый шаг
   - Подпункт 1
   - Подпункт 2
2. Второй шаг
3. Третий шаг
```

### Где посмотреть, кто часто запрашивает ошибки?

В таблице `upos_error_request_log` хранятся все запросы с `user_id`. Вы можете построить аналитику:

```sql
SELECT user_id, COUNT(*) AS requests
FROM upos_error_request_log
WHERE request_timestamp > UNIX_TIMESTAMP(DATE_SUB(NOW(), INTERVAL 30 DAY))
GROUP BY user_id
ORDER BY requests DESC
LIMIT 10;
```

### Можно ли добавить изображения к кодам ошибок?

В текущей версии модуля изображения не поддерживаются. Однако вы можете модифицировать таблицу `upos_error_codes`, добавив поле `image_url` или `image_path`, и изменить функции в [upos_error_bot_part.py](upos_error_bot_part.py) для отправки фото.

---

## Разработка и расширение

### Добавление новых функций

Модуль построен на ConversationHandler и легко расширяется:

1. Добавьте новое состояние в константы
2. Создайте обработчик функции
3. Добавьте состояние в ConversationHandler
4. Обновите клавиатуры и сообщения

### Архитектура

```
Telegram Bot API
      ↓
upos_error_bot_part.py (обработчики)
      ↓
База данных MySQL ←→ Статистика
```

Все операции с БД выполняются через context manager для безопасности транзакций.

---

## Контакты и поддержка

**Автор модуля:** SberService  
**Версия:** 1.0.0  
**Дата создания документации:** 26 января 2026 г.

При обнаружении ошибок или предложений по улучшению создавайте issue в репозитории проекта.

---

**Связанные файлы:**
- [upos_error_bot_part.py](upos_error_bot_part.py) — основная логика
- [keyboards.py](keyboards.py) — клавиатуры
- [messages.py](messages.py) — сообщения
- [settings.py](settings.py) — настройки
- [scripts/upos_error_setup.sql](../../../../scripts/upos_error_setup.sql) — инициализация БД
