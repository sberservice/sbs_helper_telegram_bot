# Модуль обработки скриншотов

Модуль для обработки скриншотов карт Спринта с наложением маркеров локации.

## Описание

Модуль принимает скриншоты приложения Спринт (Яндекс Карты) и обрабатывает их:

- Автоматически определяет светлый/тёмный режим по пикселю логотипа Яндекс Карт
- Проверяет, что на скриншоте нет существующего маркера локации
- Накладывает случайный маркер (точку или треугольник) вблизи центра экрана
- Учитывает границы интерфейса в тёмном режиме
- Обрабатывает изображения асинхронно в фоновом процессе

## Возможности

### Для пользователей

- **📸 Отправить скриншот** — загрузка скриншота для обработки
- **❓ Помощь по скриншотам** — инструкция по использованию
- Очередь заданий — изображения обрабатываются в порядке поступления
- Валидация — проверка размера и формата перед обработкой

### Обработка изображения

1. Определение режима (светлый/тёмный) по цвету пикселя логотипа «Я»
2. Проверка на наличие существующих маркеров (круглый или треугольный)
3. Определение границ карты (рамка интерфейса)
4. Случайное размещение маркера вблизи центра (±10% отклонение)
5. Наложение маркера из ассетов и отправка результата

## Установка

Таблица `imagequeue` создаётся основной схемой:

```bash
mysql -u root -p < schema.sql
```

## Архитектура

Модуль работает как два процесса (запускаются из `run_bot.py`):

```
run_bot.py
├── Telegram Bot Process     ── принимает скриншоты, добавляет в очередь
└── Image Queue Processor    ── обрабатывает очередь, отправляет результат
```

### Очередь заданий (imagequeue)

| Статус | Описание |
|--------|----------|
| 0 | Ожидает обработки |
| 1 | В обработке |
| 2 | Завершено |

При запуске процессор очищает зависшие задания (статус = 1).

## Определение режима

### Светлый режим
- Цвет пикселя логотипа: `(245, 5, 5)` — ярко-красный
- Маркер локации: зелёный круг `(145, 225, 67)` или оранжевый треугольник `(214, 126, 5)`

### Тёмный режим
- Цвет пикселя логотипа: `(150, 5, 5)` — тёмно-красный
- Маркер локации: зелёный круг `(95, 139, 52)` или оранжевый треугольник `(129, 77, 5)`
- Учитывается граница тёмного интерфейса: `(17, 29, 41)` — тёмно-синий

## Коды ошибок

| Код | Описание |
|-----|----------|
| ERR_TOO_SMALL | Изображение слишком маленькое (< 100×100) |
| ERR_NO_TRIGGER_PIXEL | Не найден логотип Яндекс Карт |
| ERR_ALREADY_HAS_CIRCLE | Уже есть круглый маркер (светлый) |
| ERR_ALREADY_HAS_DARK_CIRCLE | Уже есть круглый маркер (тёмный) |
| ERR_ALREADY_HAS_TRIANGLE | Уже есть треугольный маркер (светлый) |
| ERR_ALREADY_HAS_DARK_TRIANGLE | Уже есть треугольный маркер (тёмный) |
| ERR_FILE_NOT_FOUND | Файл не найден |
| ERR_UNKNOWN_FORMAT | Неподдерживаемый формат |

## Ограничения

| Параметр | Значение |
|----------|----------|
| Максимальный размер файла | 4 МБ |
| Минимальный размер изображения | 100×100 пикселей |
| Допустимое отклонение цвета | ±5 единиц интенсивности |
| Отклонение маркера от центра | ±10% |

## Меню пользователя

| Кнопка | Действие |
|--------|----------|
| 📸 Отправить скриншот | Загрузка скриншота для обработки |
| ❓ Помощь по скриншотам | Инструкция по использованию |

## Структура модуля

```
vyezd_byl/
├── __init__.py
├── vyezd_byl_bot_part.py    # Telegram-хендлеры (приём скриншотов)
├── processimagequeue.py     # Фоновый обработчик очереди (отдельный процесс)
├── keyboards.py             # Клавиатуры
├── messages.py              # Сообщения
├── settings.py              # Настройки (цвета, размеры, параметры)
└── README.md                # Документация
```

### Внешние ресурсы

- `assets/` — изображения маркеров для наложения
- `images/` — временное хранилище загруженных скриншотов

---

**Версия:** 1.0.0  
**Обновлено:** Февраль 2026
